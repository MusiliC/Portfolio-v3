import styles from '@/style'
import React from 'react'

const ContactComponent = () => {
  return (
    <section
      className={`${styles.paddingY} w-5/6 mx-auto flex gap-10 flex-col md:flex-row`}
    >
      {/* image */}
      <div></div>

      {/* form */}
      <div></div>
    </section>
  );
}

export default ContactComponent